{
  "name": "vente-informatique-ecommerce",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix client && npm install --prefix server",
    "dev": "concurrently \"npm run server --prefix server\" \"npm run dev --prefix client -- --host 0.0.0.0\"",
    "start": "concurrently \"npm run server --prefix server\" \"npm run dev --prefix client -- --host 0.0.0.0\"",
    "server": "npm run server --prefix server",
    "client": "npm run dev --prefix client -- --host 0.0.0.0"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
